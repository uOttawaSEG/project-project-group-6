<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EventAttendanceManagementSystem"
        tools:targetApi="31">
        <activity
            android:name=".activities.CreateEventPage"
            android:exported="false" />
        <activity
            android:name=".activities.AdministratorPage"
            android:exported="false" />
        <activity
            android:name=".activities.OrganizerPage"
            android:exported="false" />
        <activity
            android:name=".activities.SignUpPage"
            android:exported="false"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".activities.AttendeePage"
            android:exported="false" />
        <activity
            android:name=".activities.AttendeeRequestedEventsPage"/>
        <activity
            android:name=".activities.LoginPage"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>